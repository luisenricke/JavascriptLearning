(function(){

    const name = 'Luis';
    const lastname = 'Villalobos';
    const age = 25;

    //const output = name + " " + lastname + " ( " + age + " ) ";
    //const output = `${name} ${lastname} ( ${age} )`;
    const output = `
    ${name}
    ${lastname}
    ( ${age + 1000} )
    `;
    console.log(output);

})();
